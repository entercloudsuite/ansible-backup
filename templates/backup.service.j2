[Unit]
Description=Backup system with Restic

[Service]
User=root
Group=root
Type=simple
ExecStart=/bin/bash {{ duplicity_download_path }}restic/{{utility.lower()}}_backup.sh 

[Install]
WantedBy=multi-user.target
